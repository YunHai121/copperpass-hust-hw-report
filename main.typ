// =====================================================================
// 使用示例：硬件综合训练课程设计报告
// 编译：typst compile main.typ
// 正文中改写各章内容即可；图表用 fig() / tbl() 插入并用 @label 交叉引用
// =====================================================================
#import "template.typ": *

#show: report.with(
  title: "5段流水CPU设计",
  major: "计算机科学与技术",
  class: "CS240X",
  stu-num: "U20241224",
  name: "郭德纲",
  phone: "1345565666",
  mail: "13456@qq.com",
  year: "2026",
  // signature: image("assets/signature.png", height: 0.9cm), // 手写签名（可选）
  declaration: true,
)

= 课程设计概述

== 课设目的

计算机组成原理是计算机专业的核心基础课。该课程力图以“培养学生现代计算机系统设计能力”为目标，贯彻“强调软/硬件关联与协同、以CPU设计为核心/层次化系统设计的组织思路，有效地增强对学生的计算机系统设计与实现能力的培养”的教学思想。

课程设计属于设计型实验，不仅锻炼学生简单计算机系统的设计能力，而且通过进行中央处理器底层电路的实现、故障分析与定位、系统调试等环节的综合锻炼，进一步提高学生分析问题和解决问题的能力。

== 设计任务

本课程设计的总体目标是利用FPGA以及相关外围器件，设计五段流水CPU，要求所设计的流水CPU系统能支持自动和单步运行方式，能正确地执行存放在主存中的程序的功能。

== 设计要求

+ 根据课程设计指导书的要求，制定出设计方案；
+ 分析指令系统格式，指令系统功能。
+ 根据指令系统构建基本功能部件，主要数据通路。
+ 设计出实现指令功能的硬布线控制器；
+ 调试、数据分析、验收检查；
+ 课程设计报告和总结。

== 技术指标

+ 支持@tbl-inst 前27条基本32位RISC-V指令；
+ 支持多级嵌套中断，利用中断触发扩展指令集测试程序；
+ 支持5段流水机制，可处理数据冒险，结构冒险，分支冒险；
+ 能运行教师提供的标准测试程序，并自动统计执行周期数。

#tbl(
  table(
    columns: 5,
    align: center,
    [*\#*], [*指令助记符*], [*指令类型*], [*简单功能描述*], [*备注*],
    [1], [ADD], [R], [加法], [],
    [2], [ADDI], [I], [立即数加], [],
    [3], [AND], [R], [与], [],
    [4], [SLLI], [I], [逻辑左移], [],
    [5], [SUB], [R], [减], [],
  ),
  caption: "设计支持的RISC-V指令（示例）",
  label: "tbl-inst",
)

= 总体方案设计

== 单周期CPU设计

本次我们采用的方案是微程序控制，且主、控存分开的方案，即采用微程序控制方式，实现主存储器（MM）和微程序控制存储器（CM）不共用一个存储器的方式完成方案的设计。同时在实施的过程中，采用部分电路用FPGA方式下载、部分电路用硬件搭建的方式完成……等等。

总体结构图如@fig-arch 所示。（注意设计阶段只需要简单的原理示意图）

#fig(image("assets/demo-figure.png", width: 90%), caption: "总体结构图")<fig-arch>

=== 主要功能部件

==== 程序计数器PC

XXXX思想是……式，实现主存储器（MM）和微程序控制存储器（CM）不共用一个存储器的方式完成方案的设计。同时在实施的过程中，采用部分电路用FPGA方式下载、部分电路用硬件搭建的方式完成方案的设计。

==== 指令存储器IM

自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展自行扩展。

== 中断机制设计

=== 总体设计

XXXXX……（请自行扩展修改）指令的格式设计是……指令的寻址方式设计是……

== 流水CPU设计

=== 总体设计

XXXXX……（请自行扩展修改）指令的格式设计是……指令的寻址方式设计是……

= 详细设计与实现

== 单周期CPU实现

=== 主要功能部件实现

==== 程序计数器（PC）

使用一个32位寄存器实现程序计数器PC，触发方式为下降沿触发，输入为下一条将要执行的指令的地址，输出为当前执行指令的地址。Halt为停机信号，将此控制信号通过非门取反之后和时钟相与，作为寄存器的时钟输入。

程序计数器PC的Verilog代码如下：

```verilog
always@(negedge clk,posedge clear)
begin
    if(clear)
        pc_out<=0;
    else if(!halt)
        pc_out<=pc_in;
end
```

=== 数据通路的实现

首先对于控制信号进行统计，包括各个主要部件所需要输入的控制信号，以及数据通路合并表中所示的具有多输入的主要部件需要进行输入选择的控制信号，并且对各个统计信号进行整理与编码。

= 实验过程与调试

== 测试用例和功能测试

XXXXX……（请自行扩展修改）

== 主要故障与调试

=== XXX故障

*故障现象：*执行halt指令时控制信号无法通过ID/EX接口。

*原因分析：*寄存器设置为上升沿刷新，但当D端有输入且clk变化一个周期后寄存器中的数据仍然没有改变。

*解决方案：*在控制器中给halt指令一个ALUControl信号0000，让控制信号在接口处顺利传递。

= 设计总结与心得

== 课设总结

- 完成方案总结（自行修订扩充）。
- 功能总结（自行修订扩充）。
- 其他需要总结的内容，（自行修订扩充）。

== 课设心得

本次课程设计可以说是迄今为止所有实验以及课程设计中难度最大的一门。两个星期从早到晚的不懈努力以及国庆节假期的辛苦加班才终于完成了整个课程设计的设计任务。现在再来回顾整个课程设计的点点滴滴，会发现很多值得总结和回忆的地方。

#heading(numbering: none)[参考文献]

#refs[
  DAVID A.PATTERSON(美).计算机组成与设计硬件/软件接口(原书第5版).北京：机械工业出版社.
][
  David Money Harris(美).数字设计和计算机体系结构（第二版）.机械工业出版社.
][
  谭志虎，秦磊华，吴非，等.计算机组成原理（微课版 第2版）.北京：人民邮电出版社，2025年.
][
  谭志虎，周健，周游.计算机组成原理实验指导（基于RISC-V在线实训）.北京：人民邮电出版社，2024年.
]
